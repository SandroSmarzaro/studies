#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(){

    int size, axis_x, axis_y, word[axis_x][axis_y];
    
    srand(time(NULL));

    size=rand()%16;

    for(int count=0; count<axis_x; count++){
        
        for(int count2=0; count2<axis_y; count2++){
            
            
            word[count][count2]=rand
                          
        }



    return 0;
}